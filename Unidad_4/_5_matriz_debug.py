n_filas = 3
n_columnas = 4
matriz = [] # declaro es una lista (matriz)
for i in range(n_filas): # se puede reemplazar i por _
    fila = [] # x 3
    for j in range(n_columnas): # se puede reemplazar j por _
        fila.append(0)
    matriz.append(fila) # x 4